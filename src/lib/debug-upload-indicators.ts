/**
 * When true, paint the global network-activity pill and spinning upload
 * badges without a live outbox sync. Dev-only so E2E release builds are unchanged.
 */
export const FORCE_UPLOAD_INDICATORS = false;
