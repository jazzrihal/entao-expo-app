appId: com.jazzrihal.entao
env:
  E2E_EMAIL: ${E2E_EMAIL || "alice@example.com"}
  E2E_PASSWORD: ${E2E_PASSWORD || "entao-local-dev"}
  E2E_POST_CAPTION: ${E2E_POST_CAPTION || "Golden hour from Maestro"}
---
- runFlow:
    file: ../auth/sign-in.yaml
    env:
      E2E_EMAIL: ${E2E_EMAIL}
      E2E_PASSWORD: ${E2E_PASSWORD}
- tapOn: "Home"
- assertVisible:
    id: "home-feed"
- tapOn: "New post"
# In-app sheet when camera is not yet granted (CI sim without pre-grant, or race).
- runFlow:
    when:
      visible:
        id: "new-post-camera-permission-required"
    commands:
      - tapOn:
          id: "new-post-request-camera-permission"
# System dialog after tapping Accept / requestPermission().
- runFlow:
    when:
      visible: "Allow"
    commands:
      - tapOn: "Allow"
- runFlow:
    when:
      visible: "OK"
    commands:
      - tapOn: "OK"
- extendedWaitUntil:
    visible:
      id: "camera-shutter-button"
    timeout: 20000
- tapOn:
    id: "camera-shutter-button"
- assertVisible:
    id: "new-post-preview"
- assertVisible:
    id: "new-post-captured-at"
# The form fields render in a native SwiftUI Form (List), which lazily materializes
# off-screen rows — the Caption field isn't in the accessibility tree until scrolled
# into view, so scroll before tapping it rather than relying on Maestro auto-scroll.
- swipe:
    start: "50%,70%"
    end: "50%,30%"
- tapOn:
    id: "new-post-caption"
- inputText: ${E2E_POST_CAPTION}
- tapOn: "Post"
- assertVisible:
    id: "home-feed-grid"
